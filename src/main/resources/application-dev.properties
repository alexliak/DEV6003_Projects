# Development profile - HTTP only
server.port=8080
server.ssl.enabled=false

# Override session security for development
server.servlet.session.cookie.secure=false

# Keep all other properties from main application.properties
